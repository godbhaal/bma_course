<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jon.bma_dia6">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/bma_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--Permet no perdre alguns canvis al girar la pantalla, pero es recomanable ferho a traves
        dels estats: comprovar si savedInstanceState es null..  Aquesta linia es una mala praxis.
         android:configChanges="orientation|screenSize">-->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <!-- Indicar required=true si es necessari la camera per al funcionament de la app,
    si no es necessari posar a fals pero caldrà vigilar no executar cap codi de camera als
    dispositius que no en tinguin amb: hasSystemFeature(PackageManager.FEATURE_CAMERA)-->
    <uses-feature
        android:name="android.hardware.camera2"
        android:required="true" />

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="18" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="18" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

</manifest>
