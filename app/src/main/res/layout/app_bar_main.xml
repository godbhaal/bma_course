<?xml version="1.0" encoding="utf-8"?><!--Permet coordinar els dif components de la llibreria de disseny (p ex: el boto que es mou amb el snackbar)-->
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context="com.jon.bma_dia5.MainActivity">

    <!-- Barra superrior -->
    <android.support.design.widget.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/AppTheme.AppBarOverlay">

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            app:popupTheme="@style/AppTheme.PopupOverlay" />

        <!-- Si s'afegueix aqui queda unificat tot el Toolbar, si el posem a content_main apareixera amb un sombrejat -->
        <!-- ?attr/.... es per buscar un parametre predefinit -->
        <!-- Si es vol els noms amb minuscula (no recomanat per les normes d'estil) afegir la seguent linia
        app:tabTextAppearance="@android:style/TextAppearance.Widget.TabWidget"/> -->
        <!-- Si son pocs elements, desactivar 'tabMode=scrollable' pq siguin fix-->
        <!-- Utilitzar aquest STYLE per a images+text-->
        <android.support.design.widget.TabLayout
            android:id="@+id/tab_layout"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:tabMode="scrollable"
            style="@style/MyCustomTabLayout"/>

    </android.support.design.widget.AppBarLayout>

    <include layout="@layout/content_main" />

    <!-- Botó d'accio rapida -->
    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="@dimen/fab_margin"
        android:src="@android:drawable/ic_dialog_email" />

</android.support.design.widget.CoordinatorLayout>
